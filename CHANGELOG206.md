# 更新日志

本文件记录 MEBSociety 的版本变更。

## [2.0.6]

本次更新补上了 2.0.5 里留着占位提示的 **GUI 商店系统**，属于功能新增，旧数据不受影响，直接替换插件即可。

### 新增：商店系统

商店把物品和称号放在同一张表里，用商品 ID（SID）索引，同一件商品的买价和卖价写在一起。
旧版 `MBsociety` 拆成 `sell.yml` / `purchase.yml` / `prefixshop.yml` 三个文件，
同一件商品要在两个文件里各写一遍，价格容易对不上，这次不再沿用那套结构。

新增配置文件：

- `ShopConfig.yml` —— `op是否可以管理商店`、`每页显示的商品数量`、`自动推导商品图标`
- `Shops.yml` —— 商品表，首次生成时会带两条样例商品（钻石、称号"萌新"）

商品字段：

| 字段 | 说明 |
| --- | --- |
| `类型` | `item` 物品 / `prefix` 称号 |
| `名称` | 展示名，留空则用物品自己的名字或称号顶上 |
| `物品` | 物品名，走 `StringToItemParser`，如 `diamond`。不再支持旧版 `264:0` 这种写法 |
| `数量` | 每份包含的物品个数 |
| `称号` | 称号内容，仅 `prefix` 用 |
| `购买单价` / `出售单价` | 单个物品的价格，总价 = 单价 × 数量 × 份数 |
| `可购买` / `可出售` | 上下架开关，称号恒为只卖不收 |
| `图标类型` / `图标` | `-1` 无图标（交给自动推导）/ `0` 材质路径 / `1` 网络 url |

新增指令 `/mebshop`：

```
/mebshop list <page>                                  查看全部商品
/mebshop info <shop_id>                               查看商品详情
/mebshop buy <shop_id> <times>                        购买商品
/mebshop sell <shop_id> <times>                       出售商品
/mebshop additem <item_name> <num> <buy> <sell>       新增物品商品
/mebshop addpre <prefix> <buy>                        新增称号商品
/mebshop del <shop_id>                                删除商品
/mebshop switch <shop_id> <buy/sell>                  开关商品的购买/出售
/mebshop icon <shop_id> <path/url/auto> <address>     设置商品图标
/mebshop opshop                                       开关 op 管理商店的权限
```

GUI 入口在 `/mebui` → 商店系统，分为购买物品 / 出售物品 / 购买称号 / 查看全部商品，
有管理权限时额外显示"商店管理"。商品列表按 `每页显示的商品数量` 分页，
翻页按钮只在真的有上下页时才出现。

实现上留意的几点：

- **先确认背包放得下再扣钱**，避免出现钱扣了东西没拿到。
- 购买称号前检查是否已拥有，不会重复塞同一个称号。
- 商品被删除后 ID 会留空档，所以下拉框传给指令的是真正的商品 ID，不是选项下标。
- 物品名解析不出来时，新增商品仍然成功但会给管理员一条警告，
  玩家侧则直接提示"无法识别"，不会抛异常。
- 新增称号商品时挡住带空格的称号，否则会被指令拆成多个参数。

### 商品按钮显示图标

商品列表的按钮现在带图标，买卖的是什么一眼能看出来。图标有三种来源，优先级从高到低：

1. `Shops.yml` 里手动配的 `图标类型` + `图标`
2. `自动推导商品图标` 为 `true` 时，按物品名拼材质路径
3. 都没有 → 不显示图标，按钮照常能点

自动推导的规则是：方块拼 `textures/blocks/<物品名>`，其他物品拼 `textures/items/<物品名>`，
物品名的归一化方式和 `StringToItemParser` 一致（去 `minecraft:` 前缀、空格转下划线、转小写）。

**这个推导只能覆盖一部分物品。** 原版里材质名和物品名不一致的情况不少
（一个物品对应多张图、材质名带后缀等），拼错的结果是那个按钮没有图标，不影响买卖。
要精确控制就用 `/mebshop icon <shop_id> path textures/items/xxx` 手动指定，
或在 GUI 的「商店管理 → 设置商品图标」里填；填 `auto` 可以恢复成自动推导。
称号商品没有对应材质，不参与推导。

### 物品匹配逻辑

商店的物品识别分两层，两层用的机制完全不同，改配置和排查问题时都要分清。

**第一层，配置 → 物品：靠名称字符串。**
`Shops.yml` 里的 `物品` 字段是纯文本，交给 `StringToItemParser::parse()` 换成物品对象。
查表前会归一化：`trim` → 去掉 `minecraft:` 前缀 → 空格转下划线 → 转小写。
所以 `diamond`、`Diamond`、`minecraft:diamond`、`white wool` 都能正确解析。
数字 ID（旧版的 `264:0`）属于另一个类 `LegacyStringToItemParser`，两者不通，不再支持。

**第二层，物品 → 背包：不看名称，比的是 `stateId` + NBT。**
名称只在第一层用一次，之后所有比对都走 `Item::equals()`，比的是数字类型 ID、状态数据和 NBT。
`equals()` 的第三个参数决定要不要比 NBT，而这个参数在核心各处的取值并不一致——
这正是买卖两侧行为不同的根源：

| 调用 | `$checkTags` 来源 | 结果 |
| --- | --- | --- |
| `canAddItem()` / `addItem()` | 硬编码 `true` | 恒比 NBT |
| `contains()` / `removeItem()` | 动态取 `$item->hasNamedTag()` | 传入物品无 NBT 时**不比** |

#### 购买

流程：解析物品 → 算总价 → 查游戏币 → `canAddItem()` → 扣钱 → `addItem()`。

`canAddItem()` 必须排在扣钱前面，否则背包满时会出现钱扣了、物品没拿到。
这一侧的匹配本来就是严格的：`getAddableItemQuantity()` 和 `internalAddItem()` 都把
`$checkTags` 硬编码为 `true`，所以"算得出放得下"和"实际塞得进"用的是同一套判断，
不会出现预检通过、真塞时因为 NBT 不同而找不到可合并槽位的情况。

#### 出售

这一侧原本有个洞。`contains()` 的 `$checkTags` 取的是**传入物品**的 `hasNamedTag()`，
而商店物品是刚解析出来的、不带 NBT，于是 NBT 整个不参与比对——
附魔过或改过名的物品会被当成素物品按素价收走。
最容易踩到的是插件自己发的导航书：把 `book` 配成可出售商品后，玩家能把导航书当普通书卖掉。

现在不再用 `contains()` / `removeItem()`，改为自己遍历背包，
用 `equals($item, true, true)` 强制连 NBT 一起比，只统计和扣除干净的物品。
扣除时逐槽位减 count 后写回（`getItem()` 返回的是克隆，不写回不生效），
count 减到 0 时核心会自动把槽位置为空气。

同时保留一次"不比 NBT"的统计，用来区分两种失败：

- 同类物品本身就不够 → `§c你的物品数量不足！`
- 同类够、但干净的不够 → `§c该物品被修改过，无法出售！`

分开提示是因为后者如果笼统报"数量不足"，玩家看着满背包钻石会以为插件坏了。

已验证三条：干净物品可卖；带自定义名的同类物品不可卖；该物品仍能被识别为同类，
所以两种提示能正确区分。

**注意**：判定标准是"有没有 NBT"，不是"是不是附魔品"。
玩家自己给物品改过名、或物品带任何 NBT，都会被拒收。

### Bug 修复

- `ArrayPage` 在元素个数刚好被每页数量整除时多报一页（例如 10 条、每页 5 条会算成 3 页），
  翻到那一页会取到不存在的下标；空数组现在也按 1 页算，不再出现"页码不合理(1~0)"这种提示。
  受影响的还有营地入营申请、世界列表、op 列表、vip / svip 列表和游戏币排行榜的分页。
- `Players::getPlayerAllPrefixes` 在玩家称号表缺失时返回 `false`，
  调用方的 `foreach` / `in_array` 会报类型错误，现在统一返回空数组。
- `Players::addPrefix` 在玩家称号表还没建好时 `array_push` 会报错，现在先补一个空数组。